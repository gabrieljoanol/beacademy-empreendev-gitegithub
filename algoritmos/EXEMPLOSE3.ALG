 Algoritmo "ExemploSE3" //nome do programa
 VAR // definiçao das variaveis
  nome : caractere;
  salario : real;
 INICIO
  escreva("Digite o nome:")
  leia(nome)
  escreval("digite seu salario:")
  leia(salario)
  se (salario>1200) entao
  escreval(nome," seu salário é maior que o mínimo!!!")
  senao
  se(salario=1200) entao
  escreval(nome," seu salário é igual ao mínimo!!!")
  senao
  escreval(nome," seu salário é menor que o mínimo!!!")
  fimse
  fimse fimse
 fimalgoritmo